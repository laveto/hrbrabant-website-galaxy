<?php

/**
 * Here you can inject routes into an existing Concord module.
 *
 * It will get the same route config as stated in the config/modules.php['routes']
 */
Route::concordModuleGroup('website', function () {
});
